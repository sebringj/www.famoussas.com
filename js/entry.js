$('form.sign-in').submit(function (ev) {
    ev.preventDefault();
    alert('under development');
});
$('form.sign-up').submit(function (ev) {
    ev.preventDefault();
    alert('under development');
});